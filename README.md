# Teste GitHub Live

Este é um aplicativo web monolítico de lista de tarefas (to-do list) para uso pessoal. Ele consiste em um frontend React que interage com um backend Node.js/Express, utilizando SQLite para persistência local dos dados das tarefas. A arquitetura foca na simplicidade e na execução local para gerenciar tarefas diárias.

## Stack Tecnológica

- **Frontend:** React
- **Backend:** Node.js/Express
- **Banco de Dados:** SQLite
- **Infraestrutura:** Local (execução pelo usuário)
- **Arquitetura:** Monolito
- **API:** REST

## Pré-requisitos

Para rodar este projeto, você precisará ter instalado em sua máquina:

- Node.js (versão 14 ou superior)
- npm (gerenciador de pacotes do Node.js)

## Como Instalar Dependências

### Backend
No diretório `backend`:
```bash
npm install
```

### Frontend
No diretório `frontend`:
```bash
npm install
```

## Como Rodar em Desenvolvimento

### Backend
No diretório `backend`:
```bash
npm start
```
O servidor backend será iniciado na porta `3001` (ou outra porta configurada).

### Frontend
No diretório `frontend`:
```bash
npm start
```
O aplicativo frontend será iniciado na porta `3000` (ou outra porta configurada) e abrirá automaticamente no seu navegador.

## Como Executar Testes

(Ainda não implementado neste sprint)
